<%= form_with(model: [ @post, @comment ], url: user_post_comments_path(user_id: current_user.id, post_id: @post.id), local: true ) do |form| %>
  <%= form.text_area :text, hide_label: true, placeholder: "Leave a comment...", required: true %>
  <%= form.submit "Add comment" %>
<% end %>